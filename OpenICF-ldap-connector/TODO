* Should check if searched entry is under base contexts in LdapSearch.

* Work out a way to let IDM specify DN mapping for groups' owner and uniqueMember attributes.

* Support the groupMemberAttribute configuration property.

* Operational attributes.

* Talk to Scott Page about proxied authorization control and admin/self-password change

* Which attribute does the user name passed to AuthenticateOp map to?

* handle Password Expired Control (see LDAPRAB.authenticate_core())

* Add configuration property for password attribute.

* src/overview.html
